nobs.heckitrob <- function(object, ...)
{
  return(length(object$stage1$fitted))
}
nobs.heckit5rob <- function(object, ...)
{
  return(length(object$stage1$fitted))
}

nobs.etregrob <- function(object, ...)
{
  return(length(object$stage1$fitted))
}
